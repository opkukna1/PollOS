name: pollos
description: Narrative Build & Break System.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  flutter_riverpod: ^2.4.0
  
  # UI & Fonts
  google_fonts: ^6.1.0
  font_awesome_flutter: ^10.6.0
  
  # Auth & Utilities
  google_sign_in: ^6.1.5
  intl: ^0.18.0
  uuid: ^4.0.0  # <-- ADDED THIS (Zaroori hai ID generation ke liye)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

